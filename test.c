//
// Created by Evan on 18/03/2024.
//
/*int main(){
    int test[SIZE] = {1,2,4,6,4};
    display_comb(test);
    printf("\n The result of the function is  %d", read_comb(test));

    printf("\n Combination read: ");
    for (int i = 0; i < 5; i++) {
        printf("%d", test[i]);
    }
    printf("\n");

    // Generate a random combination
    random_comb(test);

    // Print the generated combination
    printf("Random combination: ");
    for (int i = 0; i < SIZE; i++) {
        printf("%d ", test[i]);
    }
    printf("\n");

    int comb[] = {5, 4, 8, 7, 2};
    int propos[] = {8, 4, 8, 2, 3};

    Wp well_placed_count = well_placed(comb, propos);

    printf("Number of well-placed digits: %d\n", well_placed_count.nbr);
    printf("(only ");
    for(int i = 0; i < well_placed_count.nbr_val; i++) {
        printf("%d ", well_placed_count.val[i]);
    }
    printf(" is (are) well placed)");

    Awp at_wrong_place_count = at_wrong_place(comb, propos);

    printf("\n\nNumber of at wrong place digits: %d\n", at_wrong_place_count.nbr);
    printf("(only ");
    for(int i = 0; i < at_wrong_place_count.nbr_val; i++) {
        printf("%d ", at_wrong_place_count.val[i]);
    }
    printf("is (are) are present but at the wrong place");

    return 0;
}*/